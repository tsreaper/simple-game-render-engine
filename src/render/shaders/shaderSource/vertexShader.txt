#version 400 core

in vec3 position;
in vec2 textureCoords;

out vec2 outCoords;

uniform mat4 transMatrix;
uniform mat4 cameraMatrix;
uniform mat4 projMatrix;

void main(void)
{
    gl_Position = projMatrix * cameraMatrix * transMatrix * vec4(position, 1.0);
    outCoords = textureCoords;
}
